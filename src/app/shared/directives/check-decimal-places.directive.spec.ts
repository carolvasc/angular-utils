import { CheckDecimalPlacesDirective } from './check-decimal-places.directive';

describe('CheckDecimalPlacesDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckDecimalPlacesDirective();
    expect(directive).toBeTruthy();
  });
});
